services:
  - type: web
    name: hana-youssef
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      - key: LOVE_PASSWORD
        sync: false
